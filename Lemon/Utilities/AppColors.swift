//
//  AppColors.swift
//  Lemon
//
//  Created by Shaluka Hewapatha on 06/01/2026.
//

import SwiftUI

extension Color {
    // The signature "Neon Lemon" color
    static let lemonPrimary = Color(red: 0.85, green: 1.0, blue: 0.0)
    
    // Dark shades for backgrounds and cards
    static let lemonDark = Color(red: 0.1, green: 0.1, blue: 0.1)      // Used for main card backgrounds
    static let lemonBackground = Color(red: 0.05, green: 0.05, blue: 0.05) // Used for app root
    static let lemonSurface = Color(red: 0.15, green: 0.15, blue: 0.15)    // Used for secondary elements
    
    // Text colors
    static let lemonSecondaryText = Color.gray
}
